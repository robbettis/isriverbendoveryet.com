{
  "dependencies": {
    "moment": "^2.22.1",
    "normalize.css": "^8.0.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.8.1",
    "parcel-plugin-assets-list": "^1.4.0",
    "prettier": "^1.12.1"
  },
  "scripts": {
    "dev": "parcel watch src/index.html",
    "dist": "parcel build src/index.html --no-source-maps --detailed-report",
    "lint": "prettier --write src/*.js src/*.css src/**/*.js",
    "serve": "parcel serve src/index.html"
  }
}
